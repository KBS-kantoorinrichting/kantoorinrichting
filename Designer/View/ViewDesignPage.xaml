<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Designer"
      xmlns:ViewModel="clr-namespace:Designer.ViewModel" x:Class="Designer.View.ViewDesignPage"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewDesignPage">
    <Page.DataContext>
        <ViewModel:ViewDesignViewModel/>
    </Page.DataContext>
    <Grid>
        <TabControl x:Name="Tabs" Margin="540,20,0,0" HorizontalAlignment="Left" Width="260">
            <TabItem Header="Catalogus">
                <Grid Background="#FFE5E5E5" Height="410">
                    <ScrollViewer>
                        <ItemsControl
                            x:Name="CatalogusList" 
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            ItemsSource="{Binding ProductList}" Width="237" Height="579">

                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border
                                        Margin="5"
                                        BorderThickness="0.2"
                                        BorderBrush="Black">
                                        <StackPanel Orientation="Vertical" Width="108" Height="108">
                                            <TextBlock
                                            Text="{Binding Name}"
                                            HorizontalAlignment="Center"                               
                                            VerticalAlignment="Top"
                                            FontSize="12"
                                            TextWrapping="Wrap"
                                            TextAlignment="Center"
                                            FontWeight="DemiBold"
                                            Width="108" Height="20"/>
                                            <Border
                                                BorderThickness="0.2"
                                                BorderBrush="Black"
                                                Height="68" 
                                                Width="68">
                                                <Image Source="..\Resources\Images\placeholder.png"/>
                                            </Border>
                                            <TextBlock
                                            Text="{Binding Price}"
                                            HorizontalAlignment="Center"                               
                                            VerticalAlignment="Top"
                                            FontSize="12"
                                            TextWrapping="Wrap"
                                            TextAlignment="Center"
                                            FontWeight="DemiBold"
                                            Width="108" Height="20"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Overzicht">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
        <Canvas x:Name="Editor" Margin="0,20,265,0"/>

    </Grid>
</Page>
